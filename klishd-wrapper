# Wrapper to run klishd under systemd

# Create socket directory if not exists
mkdir -p /var/run/klish
chmod 755 /var/run/klish

# Start klishd in daemon mode (reads config from /etc/klish/klishd.conf)
exec /usr/local/bin/klishd -d
