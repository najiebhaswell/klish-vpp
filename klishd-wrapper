# Wrapper to keep klishd running under systemd

# Create socket directory if it doesn't exist
mkdir -p /var/run/klish
chmod 755 /var/run/klish

# Start klishd with config file
exec /usr/local/bin/klishd -c /etc/klish/klishd.conf
